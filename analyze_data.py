import myutil


def determine_score():


def analyze_this_session(masterarray):
	# Determine session averages to provide back
	# Determine session maxes
	maxarray = []


	numcols = len(masterarray[0])
	ctr = 0
	while ctr < numcols:
		myutil.max_dev_twod_array(masterarray,ctr,avg)


	# Compare to Total
	# Determine score to provide back
	# Return all data
